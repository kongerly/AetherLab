# Run the backend from backend/; copy this file to .env if overrides are needed.
# APP_ENV: development, test, production
APP_ENV=development
# LOG_LEVEL: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
